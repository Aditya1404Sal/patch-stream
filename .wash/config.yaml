version: 2.0.0

build:
  command: cargo +nightly build --workspace --target wasm32-wasip2 --release
  component_path: target/wasm32-wasip2/release/meta_json.wasm

dev:
  wasip3: true
  components:
    - name: pages-agent
      file: target/wasm32-wasip2/release/pages_agent.wasm

new:
  command: cargo fetch
